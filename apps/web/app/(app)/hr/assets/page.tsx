/**
 * Assets hub — icon chooser only. Catalog lives at /hr/assets/catalog.
 * Claims /hr/assets so it does not fall through to /hr/[id].
 */
export default function HrAssetsHubPage() {
  return null;
}

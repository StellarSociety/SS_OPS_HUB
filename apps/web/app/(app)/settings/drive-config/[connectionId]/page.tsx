/**
 * Connection home — inner subtabs (Connection / folders) start deselected.
 * Panels only mount on `/connection` or `/folders/…`.
 */
export default function SettingsDriveConnectionIndexPage() {
  return null;
}

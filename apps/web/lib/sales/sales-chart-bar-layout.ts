/** Layout for grouped bar charts (e.g. current vs previous period). */
export const groupedBarChartLayout = {
  /** Gap between the paired bars within each category. */
  barGap: 0,
  /** Gap between category groups on the x-axis. */
  barCategoryGap: "6%",
} as const;

/** Max size for the original file picked before client-side crop/WebP export. */
export const USER_AVATAR_MAX_UPLOAD_BYTES = 5 * 1024 * 1024;

/** Square export size before the server stores a 256px WebP. */
export const USER_AVATAR_CROP_OUTPUT_PX = 512;

export const USER_AVATAR_ACCEPT =
  "image/png,image/jpeg,image/webp,image/gif,image/avif";

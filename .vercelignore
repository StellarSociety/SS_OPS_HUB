# Keep local-only files out of CLI deploys (vercel deploy uploads the folder).
.env
.env.*
!.env.example
Arattai.app
node_modules
.next
.git
# Root-only local data imports (do not use bare `data` — it excludes App Router pages like hr/staff/data).
/data
